---
# yamllint disable rule:line-length
format: 1
zlib:
  defaults:
    group: Libraries
    overlay: base
    relstage: stable
    urls:
      - url: https://zlib.net/$P-${V_PKG}.tar.gz

  shasums:

  versions:
    1.3.1:
      variants:
        - overlay: base
          relstage: unstable
